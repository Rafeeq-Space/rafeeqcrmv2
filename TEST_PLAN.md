# خطة اختبار رفيق CRM

## المشروع = تطبيق واحد، 3 بوابات، حسب الدومين

كل حاجة بتتحدد من الدومين اللي المستخدم داخل بيه. اللي بيوزّع ده هو ملف واحد: `proxy.ts` — بيشتغل قبل أي صفحة.

**مصطلحين لازم تفهمهم الأول:**
- **Redirect** = الرابط في المتصفح بيتغيّر فعليًا.
- **Rewrite** = المحتوى بيتغيّر بس الرابط فاضل زي ما هو.

### البوابات الأربعة

| البوابة | إمتى تدخل هنا | إيه اللي بيتفحص | النتيجة |
|---|---|---|---|
| **مسارات عامة** | `/api/*`, `/f/*`, `/set-password`, `/account-suspended` | مفيش فحص خالص | يكمل عادي |
| **سوبر أدمن** | الدومين الرئيسي + `/saas/*` | مسجل دخول؟ دوره `super_admin`؟ | لأ → redirect لـ `/logininin` |
| **مدير الشركة** | `sub.rafeeqcrm.com/admin` | الشركة موجودة؟ موقوفة؟ مسجل دخول؟ دوره صح؟ الشركة بتاعته هي نفسها؟ | فشل → 404 / صفحة إيقاف / redirect لـ `/login` — نجح → rewrite لـ `/client-admin/*` |
| **الموظف العادي** | أي مسار تاني على `sub.rafeeqcrm.com` | نفس فحوصات مدير الشركة + حساب الموظف نفسه موقوف؟ | فشل → نفس اللي فوق — نجح → rewrite لـ `/app/*` |

### جدول الـ Routes

| المسار اللي بيكتبه المستخدم | بيوصل فعليًا لـ | مين يقدر يدخل |
|---|---|---|
| `rafeeqcrm.com` | صفحة "قريبًا" | الكل |
| `rafeeqcrm.com/logininin` | دخول السوبر أدمن | غير مسجل |
| `rafeeqcrm.com/saas/*` | لوحة السوبر أدمن | super_admin |
| `sub.rafeeqcrm.com/login` | دخول الشركة | غير مسجل |
| `sub.rafeeqcrm.com/admin/*` | `/client-admin/*` | client_admin / sales_manager (نفس الشركة) |
| `sub.rafeeqcrm.com/*` (أي حاجة تانية) | `/app/*` | client_user وباقي الأدوار (نفس الشركة) |
| `sub.rafeeqcrm.com/f/[formId]` | فورم عام لالتقاط ليدز | الكل (عام) |

## خلاصة الـ proxy (`src/proxy.ts`)

`proxy.ts` هو middleware — كود بيشتغل على **كل طلب** قبل أي صفحة، وبيقرر: يكمل الطلب عادي؟ يرمي المستخدم مكان تاني (redirect)؟ ولا يقدّم محتوى مختلف بنفس الرابط (rewrite)؟

**الخطوات بالترتيب:**

1. **يستخرج اسم الشركة (subdomain)** من الدومين. لو الطلب على `rafeeqcrm.com` نفسه، الـ subdomain بيبقى فاضي.
2. **يسيب المسارات العامة تعدي فورًا** من غير أي فحص: `/api/*`، `/f/*`، `/set-password`، `/account-suspended`.
3. **لو على الدومين الرئيسي و`/saas/*`** → يتأكد إن فيه مستخدم مسجل دخول ودوره `super_admin`، وإلا يرميه على `/logininin`. وأي محاولة فتح `/admin` على الدومين الرئيسي مباشرة → 404.
4. **لو على `sub.rafeeqcrm.com/admin`** → يتأكد الشركة موجودة (وإلا 404) ومش موقوفة (وإلا يودّي لصفحة "الحساب موقوف")، بعدين يتأكد من تسجيل الدخول والدور (`client_admin`/`client_sales_manager`) وإن الشركة بتاعته هي نفسها، وأخيرًا يعمل **rewrite** لـ `/client-admin/*`.
5. **أي مسار تاني على `sub.rafeeqcrm.com`** → نفس الفحوصات (الشركة + الدخول + عزل الشركات)، وكمان يتأكد إن حساب الموظف نفسه مش موقوف شخصيًا، ثم يعمل **rewrite** لـ `/app/*`.
6. في كل مرحلة، لو مستخدم في مكانه الغلط (سوبر أدمن على subdomain، أو مدير شركة فاتح `/app`) → بيترمى تلقائيًا لبوابته الصح.

**الفايدة من كل ده**: نفس المسار (مثلاً `/dashboard`) بيوصل لمكان مختلف تمامًا حسب مين بيدخل ومن أي دومين — من غير ما نكرر أي كود.

---

## صفحة السوبر أدمن (`/saas/dashboard`) — المحتوى والصلاحيات

الصفحة شاشة واحدة مبنية من 3 أجزاء:

1. **بطاقات إحصائية عامة** — أرقام إجمالية على كل الشركات مع بعض: عدد العملاء (الشركات)، عدد المستخدمين، عدد الحملات، عدد الليدز، عدد المكتملة (converted)، ونسبة التحويل الكلية.
2. **جدول "تحليلات العملاء"** — نفس الأرقام لكل شركة على حدة (حملات، ليدز، مكتملة، مرفوضة، نسبة إكمال، آخر 30 يوم، مستخدمين). **للعرض بس، مفيش أي إجراء هنا.**
3. **جدول "العملاء" (الشركات)** — هنا كل الصلاحيات الفعلية. مقسوم لجدولين: الشركات النشطة، والشركات "بانتظار التفعيل" (اتبعتلها دعوة ولسه ما حطتش كلمة سر).

### الصلاحيات

| الإجراء | بيعمل إيه بالظبط |
|---|---|
| **+ إضافة عميل** | بيعمل شركة جديدة (`tenants`) + بيبعت دعوة بالبريد للعميل يحط كلمة سره بنفسه (مفيش باسورد بيتحدد من هنا) — بيتعمله تلقائيًا حساب `client_admin` مرتبط بالشركة |
| **تعديل** (✏️) | يغيّر اسم الشركة، البريد الإلكتروني، وكلمة السر — بيتطبّق على حساب الـ `client_admin` بتاع الشركة |
| **إعادة تعيين كلمة السر** (🔑) | نفس فكرة التعديل بس مركّزة بس على الباسورد — وبيمسح الـ 2FA بتاع الحساب تلقائيًا (يضطر يعمل scan جديد) |
| **إيقاف / تفعيل** (⏸️/▶️) | يوقف الشركة كلها: يحظر كل حسابات موظفيها (logout فوري من عندهم كلهم)، يعطّل الدومين بالكامل، من غير ما يمسح أي بيانات. لازم يختار سبب من 3 (تحديث عام / إيقاف الحساب / إيقاف كلي) بيظهر لموظفي الشركة |
| **حذف** (🗑️) | حذف نهائي — بيمسح كل حسابات موظفي الشركة + الشركة نفسها من قاعدة البيانات (بيانات فعلية بتتمسح، عكس الإيقاف) |
| **تسجيل الخروج** | خروج السوبر أدمن نفسه من حسابه |

### حدود صلاحيات السوبر أدمن

- **مالوش `tenant_id`** — مش تابع لأي شركة، وده اللي بيخليه يشوف كل الشركات مع بعض (بيستخدم اتصال خاص بقاعدة البيانات بيتخطى قيود الصلاحيات العادية).
- **مفيهوش دور تاني غيره** — لو حد بدور تاني حاول يفتح `/saas/dashboard`، الـ `proxy.ts` بيرميه بره فورًا.
- **لازم يعمل 2FA زي أي حد تاني** (قبل كده كان مستثنى).
- **مالوش وصول مباشر لبيانات الليدز التفصيلية بتاعة شركة معينة** — بس شايف أرقام إجمالية/تحليلية، مش بيدخل على ليدز شركة بعينها كأنه موظف فيها.
